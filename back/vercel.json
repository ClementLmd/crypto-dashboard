{
  "version": 2,
  "buildCommand": "cd .. && pnpm run build-shared && cd back && pnpm run build",
  "installCommand": "cd .. && pnpm install",
  "builds": [
    {
      "src": "dist/app.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "../shared/dist/**",
          "../shared/package.json",
          "../pnpm-workspace.yaml",
          "../package.json"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/app.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
